{
  "name": "aqua-portfolio",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @aqua/web dev",
    "build": "pnpm --filter @aqua/web build",
    "lint": "pnpm -r lint",
    "preview": "pnpm --filter @aqua/web preview"
  },
  "engines": {
    "node": ">=20"
  }
}
